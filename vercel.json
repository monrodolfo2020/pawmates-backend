{
  "buildCommand": "npm run build && npm run migration:run:pawmates-api",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  }
}
